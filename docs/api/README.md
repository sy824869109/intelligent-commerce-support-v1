# API 文档

保存生成后的 OpenAPI、流式事件协议、错误码和示例。M02.3 开始建立正式契约，代码和文档必须从同一事实来源生成。

生成正式合同前，先遵循 [接口、事件、幂等与失败恢复标准](../implementation-standards/v1/03_接口事件_幂等与失败恢复.md)。其中操作名和 JSON 是目标语义示例，不代表当前已有可调用端点；正式 OpenAPI/Schema 与实现必须同步验证。
