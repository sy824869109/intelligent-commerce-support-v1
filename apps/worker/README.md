# worker

异步任务执行单元。

- 负责：Outbox 投递、知识解析和索引、会话摘要、评测、清理和数据闭环任务。
- 依赖：消费有版本的事件合同；任务必须可重试、可幂等、可观测。
- 禁止：成为业务事实来源，或在没有领域服务校验的情况下改变交易状态。
- M02.2 已在 packages/persistence 建立 Outbox/Inbox 原语；当前目录尚无运行中的 Relay 或业务消费者，后续按事件合同逐步接入。
