# knowledge-service

新平台内部的 KF RAG 核心知识引擎。

- 负责：知识空间、解析、分块、索引、查询改写、检索计划、混合召回、重排、上下文、生成、引用和评测。
- 数据边界：MySQL 保存知识元数据，MinIO 保存原文，Milvus 保存向量索引。
- 兼容策略：V1 使用独立 KF 兼容锁文件；框架升级进入 V1.5 评测。
- 禁止：直接修改订单、退款、库存、物流、发票、工单或用户权限。
- 首次实现：M09.1。
