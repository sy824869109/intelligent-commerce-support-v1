# knowledge-service

新平台内部的 KF RAG 核心知识引擎。

- 负责：知识空间、解析、分块、索引、查询改写、检索计划、混合召回、重排、上下文、生成、引用和评测。
- 数据边界：MySQL 保存知识元数据，SeaweedFS S3 保存原文，Milvus 保存向量索引。
- 环境策略：按 ADR-0010 使用已有 V1 Conda 与统一哈希锁。原 KF 源码及核心检索架构不变；安全必要依赖调整先在新环境验收，不能据此宣称 Golden Set 通过。
- 禁止：直接修改订单、退款、库存、物流、发票、工单或用户权限。
- 首次实现：M09.1。
