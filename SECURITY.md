# 安全策略

## 支持范围

安全修复优先作用于 `main` 和当前活动的 `codex/v1-greenfield`。尚未合并、已废弃或个人实验分支不承诺安全维护。

## 报告方式

不要在公开 Issue、聊天记录或提交信息中披露可利用细节、真实密钥或用户数据。请优先使用 GitHub 仓库的 **Security → Advisories → Report a vulnerability** 私密报告渠道；若该功能尚未开启，请只联系仓库所有者并提供不含生产隐私的最小复现。

报告至少包含：

- 受影响版本、提交或模块；
- 风险类型、影响范围和必要前置条件；
- 最小复现步骤；
- 已确认的临时缓解措施；
- 是否可能已经发生数据泄露或密钥暴露。

## 处置原则

1. 先限制影响面并保留审计证据，再修复和回归。
2. 泄露的密钥必须轮换，不能只从 Git 历史中删除。
3. 安全修复需要权限、跨租户、日志脱敏和回归测试。
4. 生产安全修复使用独立分支和 PR；紧急情况也不得强推覆盖历史。
5. 对外公告不得包含可直接利用的细节，直到修复和升级窗口准备完成。
